import * as React from 'react';
const classnames = require('./PersonaliaSection.scss');

interface IProps {}

class PersonaliaSection extends React.Component<IProps> {
  public render() {
    return (
      <section className={classnames.root}>

      </section>
    );
  }
};

export default PersonaliaSection;
